# Teensy4.0_AsyncI2C_v0
Master only, header-only bare-bone library.
Teensy4.0 async (non-blocking) I2C using the LPI2C interrupts as it was intended.
I studied both the official library and the datasheet. Function and variable names are weird because I was writing a fantasy novel.
